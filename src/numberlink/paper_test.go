package main //same package name as source file

/*
import (
	"testing" //import go package for testing related functionality
	"fmt"
)
func TestinitTables(b *testing.B) { //benchmark function starts with "Benchmark" and takes a pointer to type testing.B
    p := NewPaper(3, 3, []string{"...","...","..."})
    fmt.Println(p.next)
}
*/
